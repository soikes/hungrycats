<div class='row'>
  remaining food on agent <%= agent.name %> (<%=agent.ip%>):
  <div id="remaining">
    <div class="anchor" style="width: <%= agent.remaining_food %>%">
      <% if 100 < 30 %>
        <span class="inner lowfood"></span>
      <% else %>
        <span class="inner"></span>
      <% end %>
    </div>
  </div>
</div>
